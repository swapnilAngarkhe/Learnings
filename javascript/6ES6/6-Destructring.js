//if you want to take an object or arrays element as a variable the old js has a long method
    //but new js use destrucre to do it.

let a=[1,2,3,4];
let [first,second]=a;
//the let frist second = a here is... first will be 1 second will be 2 ie: the index of that matches 
    // the index of array.
console.log(first,second);