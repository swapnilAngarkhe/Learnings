import pandas as pd
df=pd.read_csv # this will read any .csv file there will be option popup after typing read_ the file should be in the same folder
print(df )# to read the data. 
# there are too many various types of packages.
#you can also import package as shortuc (import pandas as pd) so that you can use them as their shortuc in code
#(df=pd.read_csv)